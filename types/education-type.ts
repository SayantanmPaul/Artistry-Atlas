export type Education = {
    _id: string,
    institution: string,
    field: string,
    courses: string[],
    location: string,
    score: string,
    startDate: Date,
    endDate: Date,
    institutionlogo: string
}